<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to("Tweeters", root_path, :class => "navbar-brand") %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li role="presentation"><%= link_to("Users", users_path) %></li>
        <li role="presentation"><%= link_to("Tweets", tweets_path) %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% unless user_logged_in? %>
        <%= link_to("Login", login_path, class: "navbar-brand") %>
        <% else %>
        <%= link_to "Logout", logout_path, method: :DELETE, class: "navbar-brand" if user_logged_in? %>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
